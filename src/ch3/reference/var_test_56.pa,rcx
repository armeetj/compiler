start:
    movq $14, -8(%rbp)
    movq -8(%rbp), %rcx
    addq -8(%rbp), %rcx
    movq -8(%rbp), %rax
    addq %rcx, %rax
    jmp conclusion

    .globl main

main:
    pushq %rbp
    movq %rsp, %rbp
    subq $16, %rsp
    jmp start

conclusion:
    addq $16, %rsp
    popq %rbp
    retq
