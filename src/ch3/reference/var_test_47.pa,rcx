start:
    movq $1, -8(%rbp)
    addq $1, -8(%rbp)
    movq $1, %rcx
    addq -8(%rbp), %rcx
    movq $1, -8(%rbp)
    addq %rcx, -8(%rbp)
    movq $1, %rcx
    addq -8(%rbp), %rcx
    movq $1, -8(%rbp)
    addq %rcx, -8(%rbp)
    movq $1, %rcx
    addq -8(%rbp), %rcx
    movq $1, -8(%rbp)
    addq %rcx, -8(%rbp)
    movq $1, %rcx
    addq -8(%rbp), %rcx
    movq $1, -8(%rbp)
    addq %rcx, -8(%rbp)
    movq $1, %rcx
    addq -8(%rbp), %rcx
    movq $1, -8(%rbp)
    addq %rcx, -8(%rbp)
    movq $1, %rcx
    addq -8(%rbp), %rcx
    movq $1, -8(%rbp)
    addq %rcx, -8(%rbp)
    movq $1, %rcx
    addq -8(%rbp), %rcx
    movq $1, -8(%rbp)
    addq %rcx, -8(%rbp)
    movq $1, %rcx
    addq -8(%rbp), %rcx
    movq $1, -8(%rbp)
    addq %rcx, -8(%rbp)
    movq $1, %rcx
    addq -8(%rbp), %rcx
    movq $1, -8(%rbp)
    addq %rcx, -8(%rbp)
    movq $1, %rcx
    addq -8(%rbp), %rcx
    movq $1, -8(%rbp)
    addq %rcx, -8(%rbp)
    movq $1, %rcx
    addq -8(%rbp), %rcx
    movq $1, -8(%rbp)
    addq %rcx, -8(%rbp)
    movq $1, %rcx
    addq -8(%rbp), %rcx
    movq $1, -8(%rbp)
    addq %rcx, -8(%rbp)
    movq $1, %rcx
    addq -8(%rbp), %rcx
    movq $1, -8(%rbp)
    addq %rcx, -8(%rbp)
    movq $1, %rcx
    addq -8(%rbp), %rcx
    movq $1, -8(%rbp)
    addq %rcx, -8(%rbp)
    movq $1, %rcx
    addq -8(%rbp), %rcx
    movq $1, -8(%rbp)
    addq %rcx, -8(%rbp)
    movq $1, %rcx
    addq -8(%rbp), %rcx
    movq $1, -8(%rbp)
    addq %rcx, -8(%rbp)
    movq $1, %rcx
    addq -8(%rbp), %rcx
    movq $1, -8(%rbp)
    addq %rcx, -8(%rbp)
    movq $1, %rcx
    addq -8(%rbp), %rcx
    movq $1, -8(%rbp)
    addq %rcx, -8(%rbp)
    movq $1, %rcx
    addq -8(%rbp), %rcx
    movq $1, -8(%rbp)
    addq %rcx, -8(%rbp)
    movq $1, %rcx
    addq -8(%rbp), %rcx
    movq $1, %rax
    addq %rcx, %rax
    jmp conclusion

    .globl main

main:
    pushq %rbp
    movq %rsp, %rbp
    subq $16, %rsp
    jmp start

conclusion:
    addq $16, %rsp
    popq %rbp
    retq
